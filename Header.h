#pragma once
#pragma once
#pragma once
#include <iostream>
#include <vector>
#include <list>
#include "Car.h"
#include "Functors.h"
#include "Car_dealership.h"

enum search { search_BY_BRAND = 1, search_BY_PRICE, search_BY_YEAR, search_BY_VOLUME };
enum sort { sort_BY_BRAND = 1, sort_BY_PRICE, sort_BY_YEAR, sort_BY_VOLUME };

using namespace std;